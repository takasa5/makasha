<% if logged_in? %>
<div class="fixed-action-btn">
    <a class="btn-floating btn-large waves-effect waves-light pink accent-2" onclick="onClickAdd()">
        <i class="material-icons">add</i>
    </a>
</div>
<div id="modal1" class="modal" style="z-index:1003">
    <div class="modal-content">
    <h5>名曲を追加する</h5>
    <%= form_with model: Post.new do |form| %>
        <i class="material-icons" style="font-size:1rem;">music_note</i><%= form.label :song %>
        <%= form.text_field :song %>
        <br>
        <i class="material-icons" style="font-size:1rem;">person</i><%= form.label :artist %>
        <%= form.text_field :artist %>
        <br>
        <label>
            <%= check_box 'post', 'twitter', :id=>"check" %>
            <span>Twitterへ投稿</span>
        </label>
        <div class="modal-footer">
            <%= form.submit "追加", :class=>"modal-close waves-effect waves-pink btn-flat"%>
        </div>
    <% end %>
    </div>
</div>
<div class="container">
    <% if flash[:notice] %>
    <div class="notice">
        <%= flash[:notice]%>
    </div>
    <% end %>
    <div class="card">
        <div class="card-content">
            <div class="row">
            <div class="col s4 l2">
                <%= image_tag(@user.image_url.sub(/_normal/, ''), {:class => "circle responsive-img"}) %>
            </div>
            <div class="col s8 l10">
                <span class="card-title"><%= @user.name %></span>
            </div>
            </div>
        </div>
    </div>
    <ul class="tabs tabs-fixed-width" id="tabs1">
        <li class="tab" id="list-tab" onclick="onClickTab(this)">
            <a href="#list">
                <i class="material-icons" style="vertical-align:middle;">list</i>
                <span style="vertical-align:middle;">List</span>
            </a>
        </li>
        <li class="tab" id="stat-tab" onclick="onClickTab(this)">
            <a href="#stat">
                <i class="material-icons" style="vertical-align:middle;">pie_chart_outlined</i>
                <span style="vertical-align:middle;">Stat</span>
            </a>
        </li>
    </ul>
    <div id="list">
        <ul class="collection">
        <% @posts.each do |post| %>
        <li class="collection-item">
            <b><%= post.song %></b> - <%= post.artist -%><br>
            <span class="grey-text"><%= post.created_at.to_s(:posted_at) %></span>
            <span class="pink-text text-lighten-3">
                <% if post.count.to_i != 1 and post.period_before.to_i != 0%>
                    <%= post.period_before %>時間ぶり 
                <% end %>
                <%= post.count.to_s %>回目
            </span>
        </li>
        <% end %>
        </ul>
        <ul class="pagination center-align">
            <li class="disabled"><a><i class="material-icons">chevron_left</i></a></li>
            <% if @count > 4 %> <!-- ページが多い場合 -->
                <li class="pg_btn active" id="pg-btn-1">
                    <%= link_to "1", move_path(index: 0), remote: true %>
                </li>
                <li><i class="material-icons">more_horiz</i></li>
                <li class="pg-btn" id="pg-btn-<% @count.to_s %>">
                    <%= link_to @count.to_s, move_path(index: @count.to_i - 1), remote: true %>
                </li>
            <% else %> <!-- ページが少ない場合 -->
                <% (1..@count).each do |i| %>
                <% if i == 1 %>
                    <li class="pg-btn active" id="pg-btn-<%= i.to_s %>">
                <% else %>
                    <li class="pg-btn waves-effect" id="pg-btn-<%= i.to_s %>">
                <% end %>
                <%= link_to i, move_path(index: i - 1), remote: true %>
                </li>
                <% end %>
            <% end %>
            <% if @count == 1 %>
                <li class="disabled"><a><i class="material-icons">chevron_right</i></a></li>
            <% else %>
            <li class="waves-effect">
                <%= link_to move_path(index: 1), remote: true do %>
                    <i class="material-icons">chevron_right</i>
                <% end %>
            <% end %>
            </li>
        </ul>
    </div>
    <div id="stat">
        UNDER CONSTRUCTION
    </div>
</div>
<% else %>
    <%= link_to 'Twitter Login', '/auth/twitter' %>
<% end %>