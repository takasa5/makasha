<div class="container">
    <%= @user.name %>

    <% @posts.each do |post| %>

    <div class="card">
        <div class="card-content">
            <b><%= post.artist -%></b>
            <%= post.song %>
        </div>
    </div>
<% end %>

</div>
